import React, {useEffect} from 'react'
import { db } from '../utils/firebase';

export default function laptop() {

  useEffect(() => {
    
    return () => {
      
    }
  }, [])

  return (
    <div>
      laptop
    </div>
  )
}
